<!DOCTYPE html>
<html>
  <head>
    <title>Kittens API</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar">
      <div class="navbar-home">
        <%= link_to 'Kittens', root_path, class: 'navbar-title' %>
      </div>
      <div class="navbar-options">
        <%= link_to 'Add Kitten', new_kitten_path, class: 'navbar-option' %>
      </div>
    </nav>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
